```


var ptr:$Pointer<string> = $Pointer<String>()

var intList:[int] = [1,4,5,6,9,100];

for(x in intList){
	
}

var ptr:Pointer = Pointer(intList);
print("address of $0 is $1",[intList,Pointer(intList)]);
ptr.size()

var a:? = 12;

$%^&*()!@#








```
